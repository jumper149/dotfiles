#!/bin/bash

pkill --signal SIGRTMIN+11 i3blocks
pkill --signal SIGRTMIN+12 i3blocks
pkill --signal SIGRTMIN+13 i3blocks
pkill --signal SIGRTMIN+14 i3blocks
pkill --signal SIGRTMIN+15 i3blocks
pkill --signal SIGRTMIN+16 i3blocks
pkill --signal SIGRTMIN+17 i3blocks
pkill --signal SIGRTMIN+18 i3blocks
pkill --signal SIGRTMIN+19 i3blocks
pkill --signal SIGRTMIN+20 i3blocks
pkill --signal SIGRTMIN+21 i3blocks
pkill --signal SIGRTMIN+22 i3blocks
pkill --signal SIGRTMIN+23 i3blocks
